import {render,screen}from'@testing-library/react';import{describe,it,expect,vi}from'vitest';import{ModelProfilesSection}from'../../../src/ui/options/ModelProfilesSection';describe('ModelProfilesSection',()=>{it('masks stored keys and labels drafts',async()=>{const repository={list:vi.fn().mockResolvedValue([{id:'p',version:'v1',name:'P',protocol:'openai-chat',endpoint:'https://a.test',model:'m',apiKey:'secret',timeoutMs:1,capabilities:[],state:'draft'}])};render(<ModelProfilesSection repository={repository as never}/>);expect(await screen.findByText(/••••••/)).toBeInTheDocument();expect(screen.getByLabelText('API Key')).toHaveAttribute('type','password')})});
