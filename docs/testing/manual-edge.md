# Edge 人工验收清单

状态：已通过。以下结果来自 Microsoft Edge 实际运行，不以 Chromium 自动化结果代替。

## 执行记录

| 字段         | 值      |
| ------------ | ------- |
| 日期/时区    | `2026-08-09 / Asia/Singapore` |
| 测试人员     | `Codex（本机实机验收）` |
| 操作系统     | `Windows 11 专业版 10.0.26200 x64` |
| Edge 版本    | `151.0.4129.59` |
| 扩展版本     | `0.1.0` |
| Git 提交     | `af57e88` 基线及 Gate 6 发布工作树 |
| 构建目录/ZIP | `.output/chrome-mv3` / `dist/siftmark-0.1.0-chromium.zip` |
| SHA-256      | `37386e035dd3d14080ab92325f9ebbba6f87d05e20153d443146088e6e9bd996` |

结果只填 `通过`、`失败` 或 `不适用`；失败必须填写问题编号和复现说明。

## 安装与权限

| ID   | 操作与预期                                                                                        | 观察值 | 结果 | 问题 |
| ---- | ------------------------------------------------------------------------------------------------- | ------ | ---- | ---- |
| E-01 | 在 `edge://extensions` 开启开发人员模式，从记录的目录/解压 ZIP 加载；名称、版本、图标正确且无错误 | ID 固定；`UNPACKED/ENABLED`；manifest/runtime error 均为 0 | 通过 | — |
| E-02 | Edge 安装/详情权限与 README 一致，网站访问状态可见                                                | 权限列表一致；网站访问状态可切换并可见 | 通过 | — |
| E-03 | 首次设置六步可跳过并可重开继续，未配置模型仍能进入本地管理器                                      | 六步可跳过/续接；无模型时本地管理器可用 | 通过 | — |
| E-04 | 开启通知才请求可选权限；拒绝后应用内通知可用                                                      | 移除通知权限后应用内通知仍显示；PNG 系统通知成功 | 通过 | — |
| E-05 | 限制网站访问后本地书签管理和备份保持可用，页面采集明确降级                                        | `ON_CLICK` 下本地管理/备份可用，采集与缩略图显示不可用 | 通过 | — |

## 原生书签与入口

| ID   | 操作与预期                                                       | 观察值 | 结果 | 问题 |
| ---- | ---------------------------------------------------------------- | ------ | ---- | ---- |
| E-06 | 在 Edge 收藏夹中新建、重命名、移动、删除；Siftmark 与原生树一致  | 原生收藏夹事件往返同步，中文标题与顺序一致 | 通过 | — |
| E-07 | 在 Siftmark 编辑后 Edge 收藏夹立即显示同一结果                   | 编辑直接写入同一原生树，无第二份数据 | 通过 | — |
| E-08 | 使用原生 `Ctrl+D` 收藏并先于 AI 分析完成；精确重复进入审批并可合并 | 书签先保存；批准后保留既有项并移除临时副本 | 待验证 | — |
| E-09 | 右键菜单四个入口全部出现并执行正确                               | 保存页面/链接/选中文本草稿/打开管理器均成功 | 通过 | — |
| E-10 | `Ctrl+Shift+S` 与 `edge://extensions/shortcuts` 自定义快捷键生效 | 默认与自定义快捷键均生效 | 通过 | — |
| E-11 | 原生收藏产生风险方案时自动显示网页审批浮层；允许、拒绝与“与 Agent 调整”均可用 | 无常驻按钮；关闭后不残留页面控件 | 待验证 | — |

## 管理、任务与恢复

| ID   | 操作与预期                                                                                       | 观察值 | 结果 | 问题 |
| ---- | ------------------------------------------------------------------------------------------------ | ------ | ---- | ---- |
| E-12 | 管理器宽/中/窄布局无不可达控件、重叠或横向页面滚动                                               | 宽屏与 800 px 窄屏均无不可达控件、重叠或横向滚动 | 通过 | — |
| E-13 | 中文本地搜索、筛选、排序、详情保存和字段级审核均正确                                             | 中文标题/标签/笔记命中；筛选、排序、保存及审核正确 | 通过 | — |
| E-14 | 归档、回收、恢复及特殊文件夹被删除后的暂停提示正确                                               | 恢复原父级与索引；删除绑定文件夹后暂停并提示重绑 | 通过 | — |
| E-15 | 在 `edge://extensions` 停止/重新加载扩展背景 Service Worker 后，本地任务恢复且分析请求不自动重复 | 本地任务恢复；分析任务变为 `unknown` 且未重发 | 通过 | — |
| E-16 | 浏览器汇总通知不泄露标题、URL 或详细错误                                                         | 系统通知仅含任务数量和状态 | 通过 | — |

## 视觉、字体与无障碍

| ID   | 操作与预期                                                                     | 观察值 | 结果 | 问题 |
| ---- | ------------------------------------------------------------------------------ | ------ | ---- | ---- |
| E-17 | 浅/深色与舒适/紧凑组合刷新后保持，焦点和对比清晰                               | 四组合持久化；深色紧凑宽/窄截图已留证 | 通过 | — |
| E-18 | 断网后本地 Noto Sans SC、Space Grotesk、Lucide 和图标正常，无远程资源请求      | 所有字体/图标本地加载，无 HTTP(S) 资源请求 | 通过 | — |
| E-19 | AI 运行、完成、暂停/失败 Lottie 有真实图形和状态文本；减少动态效果时为静态回退 | 动画帧有像素变化；reduced-motion 下两帧相同 | 通过 | — |
| E-20 | 键盘完成 Popup、管理器、设置和模态交互，焦点不丢失或陷入                       | Tab/Shift+Tab/Enter/Escape 全流程可达 | 通过 | — |

## 备份、无痕与重装

| ID   | 操作与预期                                                                    | 观察值 | 结果 | 问题 |
| ---- | ----------------------------------------------------------------------------- | ------ | ---- | ---- |
| E-21 | Siftmark JSON 与带缩略图 ZIP 导出后可重新预览，校验和与数量通过               | JSON/ZIP 重新预览完整性通过，项目与缩略图数量正确 | 通过 | — |
| E-22 | 普通备份无 API Key；加密归档正确密码成功、错误密码/篡改失败                   | 普通备份 SHA 已记录且无 Key；错误密码/篡改均拒绝 | 通过 | — |
| E-23 | HTML 和 MarkAI 导入预览显示冲突与未知字段，默认不删除现有收藏夹               | 冲突与未知字段明确；默认保留现有收藏夹 | 通过 | — |
| E-24 | 在扩展详情开启 InPrivate 运行后可保存到共享收藏夹且不保留来源标记；关闭后受限 | 真实 InPrivate 保存进入共享原生树；关闭授权后入口受限 | 通过 | — |
| E-25 | 备份后卸载/重装，原生收藏夹保留；导入后扩展元数据和加密模型配置恢复           | 重装后存储为空、原生树保留；普通备份导入 15 项，加密配置按预览恢复 | 通过 | — |
| E-26 | 分级/全部重置预览和确认词正确，Edge 原生收藏夹始终保留                        | 预览 89 项/75.3 KB；错误确认词禁用；重置前后原生树哈希一致 | 通过 | — |

## Edge 专项

| ID   | 操作与预期                                                                 | 观察值 | 结果 | 问题 |
| ---- | -------------------------------------------------------------------------- | ------ | ---- | ---- |
| E-27 | 工具栏固定/取消固定后 Popup 均可正常打开，图标在浅深浏览器主题下清晰       | 固定后从工具栏、取消后从扩展菜单均打开 Popup | 通过 | — |
| E-28 | Edge 睡眠标签页存在时窗口批量保存不会崩溃；不可读取项进入明确失败/跳过状态 | 标签为 `discarded:true/status:unloaded`；批量处理 3 项无崩溃 | 通过 | — |
| E-29 | Edge“效率模式/减少动画”下管理器列表、Lottie 回退和任务进度仍可用           | 隔离配置启用效率模式；列表/任务可用，减少动画为静态回退 | 通过 | — |
| E-30 | `edge://favorites` 与 Siftmark 的文件夹顺序和中文标题往返无乱码            | 文件夹顺序一致；“Edge 中文标题往返”等文本无乱码 | 通过 | — |

## 结论

| 项目              | 值  |
| ----------------- | --- |
| 通过数            | `30` |
| 失败数            | `0` |
| 不适用数及理由    | `0` |
| 阻塞问题          | 无 |
| 最终结论          | 通过，可发布 Chromium 开发者模式 ZIP |
| 测试人员签名/日期 | `Codex / 2026-08-09` |
