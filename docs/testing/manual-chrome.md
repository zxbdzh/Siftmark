# Chrome 人工验收清单

状态：已通过。以下结果来自 Windows 11 上的真实 Google Chrome 验收，不以 bundled Chromium 代替。

## 执行记录

| 字段         | 值      |
| ------------ | ------- |
| 日期/时区    | `2026-08-09 / Asia/Singapore` |
| 测试人员     | `Codex（本机实机验收）` |
| 操作系统     | `Windows 11 专业版 10.0.26200 x64` |
| Chrome 版本  | `151.0.7922.76` |
| 扩展版本     | `0.1.0` |
| Git 提交     | `af57e88` 基线及 Gate 6 发布工作树 |
| 构建目录/ZIP | `.output/chrome-mv3` / `dist/siftmark-0.1.0-chromium.zip` |
| SHA-256      | `37386e035dd3d14080ab92325f9ebbba6f87d05e20153d443146088e6e9bd996` |

结果只填 `通过`、`失败` 或 `不适用`；失败必须填写问题编号和复现说明。

## 安装与权限

| ID   | 操作与预期                                                                                                  | 观察值 | 结果 | 问题 |
| ---- | ----------------------------------------------------------------------------------------------------------- | ------ | ---- | ---- |
| C-01 | 在 `chrome://extensions` 开启开发者模式，从记录的目录/解压 ZIP 加载；名称、版本、图标正确且无 manifest 错误 | ID 固定；`UNPACKED/ENABLED`；manifest/runtime error 均为 0 | 通过 | — |
| C-02 | 安装前/扩展详情显示书签和网站访问等权限；与 README 权限说明一致                                             | 书签、存储、标签页、脚本、右键菜单、闹钟及网站访问与文档一致 | 通过 | — |
| C-03 | 首次设置“权限与隐私”正确显示网页访问和通知状态                                                              | 网页访问已授权；通知未授权时明确显示可选状态 | 通过 | — |
| C-04 | 开启后台汇总通知时才弹出 `notifications` 请求；拒绝后应用内通知仍可用                                       | 拒绝后应用内通知保留；授权后 PNG 系统通知成功 | 通过 | — |
| C-05 | 将站点访问改为受限后，本地管理/搜索/导出仍可用，该站采集与缩略图明确降级                                    | `ON_CLICK` 下本地搜索/导出可用，页面分析与缩略图显示不可用 | 通过 | — |

## 原生书签与入口

| ID   | 操作与预期                                                                        | 观察值 | 结果 | 问题 |
| ---- | --------------------------------------------------------------------------------- | ------ | ---- | ---- |
| C-06 | 在 Chrome 书签管理器新建、重命名、移动、删除测试书签；Siftmark 刷新后与原生树一致 | 原生事件往返同步，中文标题与顺序一致 | 通过 | — |
| C-07 | 在 Siftmark 新建/重命名/移动书签；Chrome 书签管理器立即显示同一结果，无第二棵树   | Chrome 立即显示同一节点；`bookmarks.move` 单节点返回已修正 | 通过 | — |
| C-08 | Popup 保存 HTTP/HTTPS 页面，先出现已保存，再异步显示任务；截图/模型失败不删除书签 | 书签先落原生树，后台失败仅改变任务状态 | 通过 | — |
| C-09 | 重复 URL 默认不创建副本；更新标题和明确创建副本两个分支均正确                     | 保留、更新标题、创建副本三分支均按选择执行 | 通过 | — |
| C-10 | 右键菜单分别验证保存页面、保存链接、保存选中文本为草稿、打开管理器                | 四个入口均出现并执行；选中文本草稿正确 | 通过 | — |
| C-11 | `Ctrl+Shift+S` 保存当前页；在 `chrome://extensions/shortcuts` 改键后新按键生效    | 默认及自定义快捷键均生效 | 通过 | — |
| C-12 | 启用悬浮按钮后可保存、拖动、最小化和按网站隐藏；重置隐藏网站后恢复                | 拖动位置持久化；隐藏与重置恢复正确 | 通过 | — |

## 管理、任务与恢复

| ID   | 操作与预期                                                                                                                               | 观察值 | 结果 | 问题 |
| ---- | ---------------------------------------------------------------------------------------------------------------------------------------- | ------ | ---- | ---- |
| C-13 | 管理器宽/中/窄布局均可访问文件夹、列表和详情；无横向页面滚动或文字遮挡                                                                   | 宽屏与 800 px 窄屏均无横向滚动或遮挡 | 通过 | — |
| C-14 | 本地搜索命中中文标题/标签/笔记；未配置 Embedding 时明确显示“本地搜索”                                                                    | 中文标题、标签、笔记均命中；显示“本地搜索” | 通过 | — |
| C-15 | 应用审核提案时只修改勾选字段；外部修改造成冲突时不覆盖                                                                                   | 字段级应用正确；外部变更触发冲突保护 | 通过 | — |
| C-16 | 绑定归档/回收站后移动和恢复保持原位置；删除绑定文件夹后按钮暂停并提示重新绑定                                                            | 归档/回收恢复原父级与索引；删除绑定后暂停提示 | 通过 | — |
| C-17 | 启动可观察后台任务，在 `chrome://extensions` 的 Service Worker 检查器中停止 Worker，再打开扩展唤醒；本地任务恢复，可能计费分析不自动重发 | 本地任务恢复；分析任务变为 `unknown` 且未自动重发 | 通过 | — |
| C-18 | 开启浏览器通知后完成/暂停任务；系统通知只含状态和数量，不含标题、URL 或详细错误                                                          | PNG 系统通知仅含数量和完成/暂停/失败状态 | 通过 | — |

## 视觉、字体与无障碍

| ID   | 操作与预期                                                                             | 观察值 | 结果 | 问题 |
| ---- | -------------------------------------------------------------------------------------- | ------ | ---- | ---- |
| C-19 | 浅色/深色与舒适/紧凑四组合可切换，刷新后保持；焦点环清晰                               | 四组合持久化；深色紧凑宽/窄截图已留证 | 通过 | — |
| C-20 | 断网后 Noto Sans SC、Space Grotesk、Lucide 图标仍正常，无远程字体/图标请求             | 字体与图标均为本地资源；无 HTTP(S) 字体/图标请求 | 通过 | — |
| C-21 | AI 运行、完成、暂停/失败 Lottie 非空且有像素变化；文本状态同步                         | 连续帧像素不同；状态文本同步 | 通过 | — |
| C-22 | Windows“动画效果”关闭或 DevTools `prefers-reduced-motion: reduce` 时动画稳定为静态回退 | reduced-motion 两次截图像素完全一致 | 通过 | — |
| C-23 | 仅用键盘完成 Popup 保存、管理器搜索/选择、设置切换和对话框关闭；焦点顺序合理且不陷入   | Tab/Shift+Tab/Enter/Escape 全流程可达且无焦点陷阱 | 通过 | — |

## 备份、无痕与重装

| ID   | 操作与预期                                                                                  | 观察值 | 结果 | 问题 |
| ---- | ------------------------------------------------------------------------------------------- | ------ | ---- | ---- |
| C-24 | 导出 Siftmark JSON 和带缩略图 ZIP；重新选择文件后本地预览完整性为已验证，数量正确           | JSON/ZIP 预览校验通过，项目与缩略图数量正确 | 通过 | — |
| C-25 | 普通备份不含 API Key；加密归档正确密码可预览，错误密码和篡改文件失败关闭                    | 普通备份 SHA 已记录且无 Key；错误密码/篡改均拒绝 | 通过 | — |
| C-26 | 导入浏览器 HTML 和 MarkAI JSON，确认未知字段、冲突默认不删除/覆盖现有书签                   | HTML/MarkAI 预览正确；未知字段提示；默认保留现有 | 通过 | — |
| C-27 | 在扩展详情开启“允许在无痕模式下运行”；无痕保存进入共享原生书签，界面不记录无痕来源          | 真实无痕窗口保存到共享原生树，无来源标记 | 通过 | — |
| C-28 | 关闭无痕授权后无痕页入口不可用/受浏览器限制，普通窗口数据不受影响                           | 关闭后无痕入口被浏览器限制；普通窗口数据保留 | 通过 | — |
| C-29 | 导出普通及加密备份，卸载/重装扩展，原生书签仍在；导入备份后元数据、设置和模型档案按预览恢复 | 重装后存储为空、原生树保留；普通备份导入 13 项，加密配置按预览恢复 | 通过 | — |
| C-30 | 分级重置预览数量正确；全部重置要求确认词并保留 Chrome 原生书签                              | 预览 81 项/43.0 KB；错误确认词禁用；重置前后原生树哈希一致 | 通过 | — |

## 结论

| 项目              | 值  |
| ----------------- | --- |
| 通过数            | `30` |
| 失败数            | `0` |
| 不适用数及理由    | `0` |
| 阻塞问题          | 无 |
| 最终结论          | 通过，可发布 Chromium 开发者模式 ZIP |
| 测试人员签名/日期 | `Codex / 2026-08-09` |
