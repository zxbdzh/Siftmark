import type { RequestMetric } from '../../ai/network/request-metrics'; export function AiUsageSection({metrics}:{metrics:RequestMetric[]}){return <section><h2>本地 AI 用量</h2><p>请求 {metrics.length} 次，仅保存在本地，不上传遥测。</p><table><thead><tr><th>模型</th><th>任务</th><th>耗时</th><th>状态</th></tr></thead><tbody>{metrics.map((m)=><tr key={m.requestId}><td>{m.model}</td><td>{m.taskType}</td><td>{m.latency}ms</td><td>{m.status}</td></tr>)}</tbody></table></section>}
