import { useThemeStore } from '../theme/theme-store'; export function AppearanceSection(){const theme=useThemeStore((s)=>s.theme);const density=useThemeStore((s)=>s.density);return <section><h2>外观</h2><div className="settings-grid"><label>主题<select value={theme} onChange={(e)=>useThemeStore.getState().setTheme(e.target.value as never)}><option value="system">跟随系统</option><option value="light">浅色</option><option value="dark">深色</option></select></label><label>密度<select value={density} onChange={(e)=>useThemeStore.getState().setDensity(e.target.value as never)}><option value="comfortable">舒适</option><option value="compact">紧凑</option></select></label></div></section>}
