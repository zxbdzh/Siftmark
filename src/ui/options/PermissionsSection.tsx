import { useState } from 'react'; export function PermissionsSection(){const[enabled,setEnabled]=useState(false);return <section><h2>权限与通知</h2><p>网页权限用于读取页面内容；撤销后本地书签管理仍可使用。</p><label><input type="checkbox" checked={enabled} onChange={(e)=>{const checked=e.target.checked;if(checked)void browser.permissions.request({permissions:['notifications']}).then(setEnabled);else void browser.permissions.remove({permissions:['notifications']}).then(()=>setEnabled(false))}}/>后台任务通知</label></section>}
