export const contextMenuIds = ['siftmark-save-page','siftmark-save-link','siftmark-save-selection','siftmark-open-manager'] as const;
export async function registerContextMenus(api: typeof browser.contextMenus): Promise<void> { await api.removeAll(); await api.create({id:contextMenuIds[0],title:'智能收藏当前页面',contexts:['page']}); await api.create({id:contextMenuIds[1],title:'智能收藏此链接',contexts:['link']}); await api.create({id:contextMenuIds[2],title:'保存选中文本为笔记',contexts:['selection']}); await api.create({id:contextMenuIds[3],title:'打开 Siftmark 管理器',contexts:['all']}); }
